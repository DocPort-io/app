[server]
host = "localhost:8080"
bind = "127.0.0.1"
port = "8080"

[database]
dsn = "postgres://postgres:postgres@localhost:5432/docport-dev?sslmode=disable"

[auth]
jwksUrl = ""

[storage]
provider = "filesystem"
path = "./storage"
